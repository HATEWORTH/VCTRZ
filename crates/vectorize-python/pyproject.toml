[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "vectorize"
requires-python = ">=3.9"
description = "High-quality raster-to-vector conversion"
license = { text = "MIT" }

[tool.maturin]
features = ["pyo3/extension-module"]
manifest-path = "Cargo.toml"
